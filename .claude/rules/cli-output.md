---
paths:
  - "scripts/**"
---

When running hero CLI commands (`ruby scripts/cli.rb`), always show the full output so the player can see verification results clearly.

When a verification check fails, explain what was expected and what was found. Don't summarize -- show the actual error.
